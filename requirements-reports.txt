# Optional extras that the ``report-generation`` Pi skill needs to write
# the ``.docx`` and ``.pdf`` artefacts. The skill degrades gracefully
# (still emits ``.md`` + ``.json`` + ``.txt``) when either library is
# missing — install this file only if you want Word / PDF output.
python-docx>=1.1.0
reportlab>=4.0.0
