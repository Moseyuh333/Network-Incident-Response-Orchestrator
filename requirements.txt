# N.I.R.O. — Network Incident Response Orchestrator
# Runtime + optional dependency lock for the report-generation skill.
#
# Install core only:
#     pip install -r requirements.txt
# Install core + report.docx support (python-docx):
#     pip install -r requirements.txt -r requirements-reports.txt
# Install dev / test extras:
#     pip install -r requirements.txt -r requirements-dev.txt
#
# Notes:
#   * The README also documents a one-shot installer:
#         pip install -e .[dev,reports]
#     Use that when you want hatchling's editable mode.
#   * The ``.pi`` directory holds Pi-coding-agent resources (agents,
#     skills, prompts, chains, extensions). Run ``npm install`` at the
#     project root to install them — see docs/USAGE.md.
#
# ── Core runtime ──────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
sqlmodel>=0.0.22
pydantic>=2.7.0
pydantic-settings>=2.3.0
jinja2>=3.1.3
httpx>=0.27.0
google-genai>=1.0.0
python-multipart>=0.0.9
python-dotenv>=1.0.1
