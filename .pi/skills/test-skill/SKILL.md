---
name: test-skill
description: Test stub skill used by the validator test harness and unit tests.
---

# test-skill

This is a minimal stub skill that exists to verify the Pi resource validator
and the skill registry can handle a valid `SKILL.md` with both frontmatter and
body content. It is intentionally inert: the accompanying `test_skill.py` is a
no-op `run(ctx)` placeholder.
